<div class="flex justify-evenly my-4">
  <%= link_to t('defaults.list.edit'), edit_wish_list_path(wish_list), class: "btn btn-active btn-secondary" %>
  <%= button_to t('defaults.list.delete'), wish_list_path(wish_list), class: "btn btn-active btn-accent", method: :delete, data: { confirm: t('defaults.message.delete_confirm') } %>
</div>